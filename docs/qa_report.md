# 标签质量 QA 报告

本报告用于检查 `data/clean_reviews.csv` 的标注质量，不代表最终分析结论。

## 本轮结果

- 检查总行数：30
- 缺失列、非法枚举值、关键字段为空：未发现
- 过泛 `product_suggestion`：未发现
- 疑似无关或低信息量评论：未发现
- 建议移出 `clean_reviews.csv` 的 ID：暂无
- 建议重写 `product_suggestion` 的 ID：暂无
- 需要补充替换评论数量：0
- 建议复核的 ID：`DB_009`、`DB_010`、`DS_004`、`DS_005`、`DS_008`、`DS_009`、`KIMI_002`、`KIMI_005`、`KIMI_010`

## 需要复核的 ID

| id | 问题原因 | 建议处理 |
| --- | --- | --- |
| `DB_009` | `ai_issue_type` 为 `不明确/其他`，但评论实际指向“简单内容不能生成”的疑似安全误拦截问题。 | 可保留当前标签；后续若同类样本增多，考虑新增“安全误拦截/拒绝生成”标签。 |
| `DB_010` | 正向评论体现模糊搜索和情绪价值场景，当前 `ai_issue_type` 为 `不明确/其他`。 | 可保留作为正向场景样本；做问题分布时不要把它当作负向问题。 |
| `DS_004` | 评论指向普通问题被判违规，当前 `ai_issue_type` 为 `不明确/其他`。 | 可保留当前标签；后续若同类样本增多，考虑补充安全拦截误判标签口径。 |
| `DS_005` | QA 脚本因文本包含“隐私”触发 P2 可疑警告，但评论整体是正向评价。 | 优先级 `P2` 可保留；在报告中作为正向体验证据使用。 |
| `DS_008` | 评论是键盘误触/输入交互问题，当前 AI 问题类型没有完全对应项。 | 可保留 `不明确/其他`；若同类问题增多，补充“输入交互异常”标签口径。 |
| `DS_009` | 评论涉及写作内容隔天消失，属于数据丢失风险，但 `ai_issue_type` 为 `不明确/其他`。 | 优先级 `P0` 可保留；后续可补充“内容保存/数据丢失”标签。 |
| `KIMI_002` | 正向评论涉及总结、自然语言处理和代码辅助求证，当前 `ai_issue_type` 为 `不明确/其他`。 | 可保留作为正向能力样本；报告中用于说明用户认可的复杂任务能力。 |
| `KIMI_005` | 评论同时涉及数据反馈开关、Word 图片占位符和 LaTeX 识别，当前 `ai_issue_type` 为 `不明确/其他`。 | 可保留当前标签；后续可在标签体系中补充“文档解析能力”或“数据反馈控制”口径。 |
| `KIMI_010` | 评论包含闪退和代码生成质量问题，当前 `ai_issue_type` 为 `不明确/其他`。 | `P0` 可保留；建议在分析时优先按闪退稳定性问题处理，代码能力问题作为次要问题记录。 |

## 整体标签口径

- `ai_issue_type` 为 `不明确/其他` 的行数为 10，占比约 33%。
- 当前不建议直接移出样本；主要问题是现有 AI 问题类型尚未覆盖安全误拦截、文档解析、输入交互、内容保存等细分场景。
- 后续可在编码指南中补充更细标签，或在报告中单独说明 `不明确/其他` 的口径限制。
